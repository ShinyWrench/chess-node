{
  "name": "chess-node",
  "version": "1.0.0",
  "description": "Goal: Build a chess-playing server (node) that can be deployed in a headless swarm of many server nodes. A human-sent request to any node can begin (or end?) a tournament that is automatically played to completion by the nodes.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "minimist": "^1.2.5"
  }
}
